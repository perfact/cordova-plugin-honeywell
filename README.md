Cordova plugin for Honeywell scanners
=====================================

This plugin adds a new object, `window.HoneywellScanner`, which exposes one
function, `setScannerEnabled(enabled, success_cb, error_cb)`.

The function disables or enables the hardware scanner buttons on Honeywell
scanners, depending on the boolean variable `enabled`.
